# Lab 11: NumPy Arrays In-Class Notes

* Tasks
  * Create `Chessboard` class
    * 8x8 grid
    * Need `black` and `white` square
    * Each queen will be `red` or `blue`
  * Methods
    * `add_red`
    * `add_blue`
    * `render`
    * `is_under_attack`


* Need two libraries:
  * `import numpy`
  * `import matplotlib.pyplot as plt`
    * He had:

      ```py
      %matplotlob.pyplot as plt
      import matplotlib.pyplot as plt
      ```
